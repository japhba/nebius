# Paths are relative to this machine's $HOME and are rsynced to the Nebius
# dev box home directory after the VM is reachable.
#
# Keep project/code directories and high-churn caches out of this file.
# These paths are user-level config only. Some of them contain credentials
# (.env, .config/gh), so only sync to hosts you trust.
# Agent credentials are handled separately by scripts/sync-agent-auth.sh.

.bash_profile
.bashrc
.config/git/
.env
.gitconfig
.profile
.tmux.conf
